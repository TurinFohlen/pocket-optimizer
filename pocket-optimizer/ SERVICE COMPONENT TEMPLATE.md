 SERVICE COMPONENT TEMPLATE
（复制给任何 AI 或开发者使用）
复制代码

# ================================
# SERVICE COMPONENT TEMPLATE
# ================================

# 1️⃣ 注册信息（必须填写）

@registry.register(
    name='service.__________',        # 组件唯一命名空间
    type_='service',                  # 固定为 service
    signature='__________()'          # 服务签名描述
)

class __________________Service:
    """
    组件名称：
    ____________________________

    组件职责：
    ____________________________

    输入格式：
    ____________________________

    输出格式：
    ____________________________

    不做什么：
    ____________________________
    """

    # ================================
    # 单例模式（不可修改）
    # ================================
    _instance = None

    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance

    def __init__(self):
        if not hasattr(self, '_initialized'):
            # 内部状态变量
            # self._xxx = None
            self._initialized = True

    # ================================
    # 核心 API（必须定义）
    # ================================
    def __________________(self, __________________):
        """
        功能说明：
        ____________________________

        参数：
        ____________________________

        返回：
        ____________________________
        """
        raise NotImplementedError("Method not implemented")

    # ================================
    # 可选扩展 API（可删）
    # ================================
    def __________________(self, __________________):
        raise NotImplementedError("Method not implemented")


# ================================
# 全局实例（可选但推荐）
# ================================
__________ = __________________Service()
📌 使用规则
必须填写：
service 命名空间
class 名
signature
核心 API 方法名
输入输出说明
禁止修改：
单例结构
registry 注册方式
📌 适用范围
适用于：
所有 service.xxx 组件
所有 registry 注册模块
所有计算服务
所有 translator
所有 exporter