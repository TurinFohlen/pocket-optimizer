#!/usr/bin/env python3
# ===== 显式导入所有业务组件（强制注册）=====
import sources.test_cli          # 测量源
import sources.interactive
import sources.test_function
import algorithms.genetic       # 算法
import algorithms.pso
import algorithms.simulated_annealing
import algorithms.bayesian
import algorithms.powell
import algorithms.test_ga
import uis.cli_menu            
import uis.cli_quick
import uis.legacy_menu  
import services.distance_service  
# =========================================

import loader                  # 全库扫描（可选）
import sys
import os
from registry import registry

def main():
    skin_name = sys.argv[1] if len(sys.argv) > 1 else 'ui.cli_menu'
    ui_cls = registry.get_component(skin_name)
    if not ui_cls:
        print(f"❌ 皮肤 '{skin_name}' 不存在")
        return
    print(f"🎨 加载皮肤: {skin_name}")
    ui = ui_cls()
    ui.run()

if __name__ == '__main__':
    main()